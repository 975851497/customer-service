# 教育智能客服系统 — 项目分享串讲稿

## 开场（1分钟）

大家好，今天给大家分享一个面向在线教育行业的智能客服系统。这个项目叫 **Edu-Assist**，是一个基于大语言模型的对话式客服系统，覆盖课程咨询、订单查询、学习进度跟踪、退款申请、工单提交等教育客服场景。

传统的教育客服系统通常基于关键词匹配或固定菜单导航，用户体验机械、处理复杂问题时效率低下。我们的目标是用 LLM 重塑客服体验，让机器人能像真人客服一样理解用户意图、多轮对话收集信息、执行业务流程。

---

## 技术栈概览（1分钟）

- **后端**：Python 3.11+ / FastAPI / LangChain
- **数据库**：MySQL 8.0 + SQLAlchemy 2.0 async
- **LLM**：通义千问（OpenAI 兼容接口）
- **流程定义**：YAML 声明式
- **前端**：Vue 3 + Vite（独立项目）
- **部署**：Docker

---

## 系统架构（3分钟）

### 六层架构

整个系统分为六层，职责清晰，便于扩展和维护：

```
API 层 → 服务层 → 引擎层 → 规划/任务/知识/闲聊/澄清层 → 动作层 → 基础设施层
```

**API 层**：FastAPI 路由，处理 HTTP 请求和 SSE 流式响应。

**服务层**：DialogueService，编排核心流程——加载状态、引擎处理、持久化保存。

**引擎层**：DialogueEngine，负责会话管理、消息路由、回合生命周期控制、流式输出调度。

**规划/任务/知识/闲聊/澄清层**：这是核心的业务逻辑层。

- **规划器（TurnPlanner）**：每一条用户消息进来，先由 LLM 分析意图，输出结构化的 TurnPlan——走哪个赛道
- **任务赛道**：执行业务流程，如订单查询、退款申请
- **知识赛道**：查询教育数据 API 获取课程信息、学习进度等
- **闲聊赛道**：处理问候、感谢等日常对话
- **澄清赛道**：当 LLM 无法判断意图时主动追问

**动作层**：内置动作 + 自定义业务动作，每个动作是一个原子操作。

**基础设施层**：LLM 调用、数据库访问、HTTP 客户端、配置管理等。

### 消息处理流程

用户发消息 → 加载对话状态 → 引擎处理 → LLM 规划 → 路由到对应赛道 → 执行业务逻辑 → 生成回复 → 保存状态

---

## 核心设计亮点（5分钟）

### 1. 回合规划机制

这是系统最核心的设计。每条用户消息进来，不直接匹配意图，而是交给 LLM 做"回合规划"，输出一个结构化的 TurnPlan：

```python
class TurnPlan(BaseModel):
    task: TaskTurnPlan | None = None       # 执行业务流程
    knowledge: KnowledgeTurnPlan | None = None  # 查询知识
    chitchat: ChitchatTurnPlan | None = None    # 闲聊
```

同一回合只能走一个赛道，由 Validator 保证。这样设计的好处是：

- **灵活**：LLM 根据上下文动态决策，而不是硬编码规则
- **可扩展**：新增业务只需在提示词中注册新流程
- **容错**：规划无效时自动走澄清路径

### 2. YAML 声明式流程

业务流程用 YAML 文件定义，支持四种步骤类型：

| 步骤类型 | 说明 |
|---------|------|
| `start` | 流程起始 |
| `collect` | 收集用户输入的槽位值 |
| `action` | 执行原子业务动作 |
| `end` | 流程结束 |

一个订单查询流程只需要 5 步定义，新增流程无需写代码，改 YAML 即可。

支持条件跳转和槽位校验：

```yaml
- id: validate_phone
  type: collect
  slot_name: phone
  validation: "len(value) == 11 and value.isdigit()"
  failure_response:
    text: "请输入11位手机号码。"
```

### 3. 自动聚焦对象填充

前端发送结构化对象消息（如点击订单卡片），系统自动识别对象类型并映射到当前流程需要的槽位，无需 LLM 参与。

例如用户点击一个订单卡片，系统自动将 `order_number` 槽位填充为该订单 ID，流程继续执行。

### 4. 任务中断与恢复

支持多级任务中断/恢复，通过 `paused_tasks` 栈实现：

```
用户：我要退款
   → 启动退款流程（活跃任务）
用户：等一下，先帮我查订单 ORD123
   → 中断退款，启动订单查询
   → 订单查询结束后自动恢复退款流程
```

用户可以在多个任务间自由切换，系统自动管理上下文。

### 5. 流式输出

基于 SSE 协议实现 LLM token 级流式推送，前端打字机效果。从 LLM 的 `astream()` 到引擎层的 async generator，到服务层，再到 API 层的 StreamingResponse，全链路异步流式。

关键设计：流式输出完成后才保存状态，保证原子性。

### 6. 知识渐进式搜索

课程搜索支持关键字拆解和渐进式回退。当主关键词查不到结果时，自动拆解为子关键词再搜索：

```
"Python 全栈课程" → ["Python 全栈课程", "Python 全栈", "全栈课程", "Python", "全栈"]
```

---

## 关键挑战与解决方案（3分钟）

### 挑战 1：LLM 输出的不确定性

LLM 规划结果可能不符合预期格式或包含无效字段。

**解决方案**：引入 TurnPlanValidator，用 Pydantic 做结构化校验，无效时自动走澄清路径。

### 挑战 2：关键字搜索为空

用户搜索"Python 全栈课程"时，API 返回空结果。拆词后发现"全栈"能匹配到课程。

**解决方案**：实现 `_split_keywords()` 渐进式搜索 + 同义词兜底。

### 挑战 3：知识查询走错了赛道

用户问"Python 全栈课程大概是什么情况"，系统启动了 task 流程而不是 knowledge 查询。

**解决方案**：在 turn_plan 提示词中增加优先级规则——一般性信息查询优先走 knowledge 赛道。

### 挑战 4：流程在查询失败后继续执行

`action_lookup_course_series` 返回"未找到"后，流程仍继续问"你想了解哪个班次"，逻辑矛盾。

**解决方案**：action 返回 `slot_updates={"series_name": None}` 清除无效槽位；FlowExecutor 在 action 执行后重新评估跳转目标。

---

## 项目目录结构

```
edu-assist/
├── main.py                          # 应用入口
├── flow_config/                     # YAML 流程定义
│   ├── user_flows.yml               # 用户业务流
│   └── system_flows.yml             # 系统流程
├── edu_assist/
│   ├── conf/                        # 配置管理
│   ├── domain/                      # 领域模型
│   ├── infrastructure/              # LLM/数据库/HTTP 客户端
│   ├── repository/                  # 状态持久化
│   ├── service/                     # 对话服务编排
│   ├── engine/                      # 对话引擎
│   ├── api/                         # HTTP 路由
│   ├── plan/                        # 回合规划
│   ├── knowledge/                   # 知识问答
│   ├── chitchat/                    # 闲聊
│   ├── clarify/                     # 意图澄清
│   ├── prompts/                     # Jinja2 提示词模板
│   └── task/                        # 任务/流程/动作
```

---

## 演示场景（2分钟）

### 场景一：课程咨询 → 知识查询

```
用户：Python 全栈课程大概是什么情况？
系统：→ 知识赛道 → 搜索课程信息
     → 返回课程列表、描述、评分、在售班次及价格
     → 引导选择具体班次
```

### 场景二：多轮订单查询

```
用户：帮我查一下订单
系统：→ task 赛道 → 启动 order_status_query 流程
     → 收集："请提供订单号"
用户：ORD20240401005
系统：→ action_lookup_order → 返回订单详情
```

### 场景三：任务中断与恢复

```
用户：我要退款
系统：→ 启动 refund_request 流程
用户：先帮我查一下 ORD123
系统：→ 中断退款 → 查询订单 → 自动恢复退款流程
```

### 场景四：流式打字机效果

用户发送消息，后端实时推送 LLM token，前端逐字渲染，响应延迟从"等待完整生成"优化为"首 token 即显示"。

---

## 总结与展望（1分钟）

这个系统的核心价值在于：

1. **LLM 驱动的对话规划**——告别关键词匹配，用 LLM 理解真实意图
2. **声明式流程编排**——YAML 定义业务逻辑，新增流程零代码
3. **弹性任务管理**——支持多任务中断恢复，对话自然流畅
4. **全链路流式输出**——token 级实时推送，用户体验大幅提升

### 后续规划

- **知识库增强**：接入向量数据库，支持 FAQ 语义匹配
- **多轮上下文压缩**：长对话的 token 压缩策略
- **A/B 测试框架**：不同提示词策略的效果对比
- **监控与日志**：LLM 调用耗时、意图分布、流程完成率等指标
- **多渠道接入**：微信、企业微信、Web SDK

---

感谢聆听，欢迎提问交流！
